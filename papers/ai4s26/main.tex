\documentclass[conference]{IEEEtran}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{url}
\usepackage{multirow}

\title{Benchmarking Neural Surrogates for Hydrofoil RANS Flow\\
and Hydrodynamic Shape Optimization}

\author{\IEEEauthorblockN{Aarav Dixit}
\IEEEauthorblockA{aardixit7@gmail.com}}

\begin{document}
\maketitle

\begin{abstract}
Neural flow surrogates are usually compared by field error, although their
practical value often lies in the designs they select. We compare a
convolutional neural network (CNN) U-Net, a physics-regularized point
network, a Fourier neural operator (FNO), and a deep operator network
(DeepONet) on 381 OpenFOAM simulations of hydrofoils in water. Complete NACA
families form disjoint training, validation, and final-test sets, and every
architecture is trained with three seeds. The models predict
Reynolds-averaged Navier--Stokes (RANS) fields, lift, drag, and pressure-based
cavitation-inception risk before driving the same four-parameter shape search.
DeepONet gives the lowest mean pressure, $C_p$, lift, and drag errors on the
untouched test families; the physics-regularized network is close while using
one tenth as many parameters. Inference is 384--867 times faster than CFD.
Across seeds, fresh OpenFOAM endpoint solves give mean lift-to-drag ratios from
22.10 to 37.90, but a three-level grid study shows that drag remains
mesh-sensitive. Field accuracy, force accuracy, search quality, and numerical
verification are therefore related but not interchangeable.
\end{abstract}

\begin{IEEEkeywords}
artificial intelligence for science, CFD surrogate, neural operator, physics-informed learning, hydrofoil optimization, cavitation inception
\end{IEEEkeywords}

\section{Introduction}
For a hydrofoil operating in water, shape design is an optimization problem
wrapped around a flow solver. Each change in camber, thickness, or incidence
requires another Reynolds-averaged Navier--Stokes (RANS) solution, so even a
modest search can consume hundreds of computational fluid dynamics (CFD)
evaluations; neural surrogates move most of that search off the solver by
learning the map from geometry and operating conditions to flow and forces
\cite{brunton2020machine}. The difficult question is not simply which network
minimizes field error, but which one preserves the quantities that determine a
useful design.

The convolutional neural network U-Net (CNN-U-Net), Fourier neural operator
(FNO), deep operator network (DeepONet), and physics-regularized point network
impose very different structure on that map
\cite{ronneberger2015unet,li2021fno,lu2021deeponet,raissi2019pinn}. A
convolutional model emphasizes local multiscale features; FNO mixes information
globally in Fourier space; DeepONet separates parameter and coordinate
representations; and a physics penalty constrains a pointwise regressor. Their
relative merits may therefore change as the target moves from pressure fields
to forces and finally to shape optimization.

Airfoil-surrogate research has progressed from field prediction
\cite{guo2016steady,bhatnagar2019aerodynamic,sekar2019airfoils,thuerey2020rans}
to benchmarks that evaluate surface forces, out-of-distribution accuracy, and
computational cost \cite{bonnet2022airfrans,li2023gino,yagoubi2025ml4cfd}.
Those metrics establish whether a model approximates CFD, but they do not by
themselves establish whether it will make the same design decision. A small,
structured force error can be amplified when an optimizer repeatedly queries a
narrow region of design space. The missing experiment is a controlled
comparison that follows several surrogate families through the same optimizer
and then checks the selected geometries with new CFD solutions. High-fidelity
endpoint verification is standard in surrogate shape optimization, while a matched direct-CFD
search provides a stronger reference when its cost is manageable
\cite{li2022mlaso,shukla2023operators}.

That experiment is the contribution of this paper; we do not propose a new
architecture. We compare four representative families on the same RANS data,
complete-geometry validation split, targets, and three-start hydrodynamic shape
optimization (HSO) problem. The
benchmark connects field and force accuracy, pressure-threshold cavitation
screening, and runtime to the final geometry selected by each model. Fresh
OpenFOAM solutions measure endpoint discrepancy, and a direct OpenFOAM search
uses the same starts and optimization budget. Together, these tests ask whether
validation accuracy predicts search quality and how closely each inexpensive
search approaches the CFD-driven result.

\section{Related Work}
Convolutional surrogates established that steady flow fields can be learned
directly from geometry encodings on Cartesian grids
\cite{guo2016steady,bhatnagar2019aerodynamic,thuerey2020rans}. Later datasets
and benchmarks broadened evaluation beyond image error: AIRFRANS includes
surface quantities and out-of-distribution splits, the geometric neural
operator (GINO) addresses irregular three-dimensional geometries, and ML4CFD evaluates modern operator-learning
systems under a common protocol
\cite{bonnet2022airfrans,li2023gino,yagoubi2025ml4cfd}. These studies motivate
our geometry-level validation split and separate reporting of fields, forces,
runtime, and low-pressure classification.

Neural operators represent an entire parameter-to-solution map rather than a
single flow realization. FNO performs global spectral mixing on a regular
grid, whereas DeepONet combines a parameter-dependent branch representation
with coordinate-dependent trunk functions
\cite{li2021fno,lu2021deeponet,kovachki2023neural}. Physics-informed learning
offers a different inductive bias by penalizing differential-equation
residuals \cite{raissi2019pinn,li2024pino}. The present comparison intentionally
includes both grid operators and pointwise models, since their relative value
may change when the output is used for optimization rather than visualization.

Learning-based airfoil optimization has shown large online speedups and has
validated selected optima with higher-fidelity solvers
\cite{li2022mlaso,shukla2023operators}. Most such studies develop or test one
surrogate family. Our contribution is complementary: data, split, targets,
optimizer, starts, bounds, and endpoint solver are held fixed while the model
family changes. A matched direct-CFD search then distinguishes prediction error
from the effect of optimizing a learned objective.

\section{Data and CFD Reference}
\subsection{Design space}
The dataset contains National Advisory Committee for Aeronautics (NACA) 0006,
0008, 0010, 0012, 0015, 0018, 2412, 2415, 4412, 4415, 4418, and 4421 profiles
generated from the standard four-digit parameterization
\cite{abbott1959airfoils}. For each profile, we simulate angles of attack
$\alpha\in\{-4,-2,0,2,4,6,8,10\}^{\circ}$ and Reynolds numbers
$Re\in\{10^5,2\times10^5,5\times10^5,10^6\}$. All cases use liquid water
with density 997~kg/m$^3$ and kinematic viscosity $10^{-6}$~m$^2$/s. With a
1~m chord, the freestream speed is set by $U_\infty=Re\nu/c$; the far-field
absolute pressure and vapor pressure are 101325 and 2300~Pa.

Each case is solved with OpenFOAM \texttt{simpleFoam}
\cite{weller1998tensorial}. For steady incompressible flow, the mean velocity
$\bar{u}_i$ and pressure $\bar{p}$ satisfy
\begin{align}
\frac{\partial \bar{u}_i}{\partial x_i}&=0,\\
\rho\bar{u}_j\frac{\partial\bar{u}_i}{\partial x_j}
&=-\frac{\partial\bar{p}}{\partial x_i}
+\mu\frac{\partial^2\bar{u}_i}{\partial x_j\partial x_j}
-\rho\frac{\partial\overline{u_i'u_j'}}{\partial x_j}.
\end{align}
The Reynolds stress is closed with the $k$--$\omega$ shear-stress transport
(SST) model
\cite{menter1994sst}. Angled freestream velocity is imposed at the inlet and
far boundary, and OpenFOAM's \texttt{forceCoeffs} supplies total lift and drag
after 300 iterations. Cell-centered fields are interpolated to a common
$128\times64$ Cartesian grid over $x/c\in[-1,2]$ and
$y/c\in[-0.75,0.75]$. Inputs include coordinates, operating parameters, three
NACA descriptors, a fluid mask, and signed distance to the profile.

Three simulations with $|C_L|$ or $|C_D|>5$ are discarded before splitting,
leaving 381 cases. The split is fixed before training and is identical for all
architectures and seeds. NACA 0015 and 2412 provide 64 validation cases for
early stopping; NACA 0018 and 4418 provide an untouched 64-case test set used
only for final reporting. The remaining 253 cases from eight families train
the models. No validation or test geometry therefore appears in training at a
different operating condition.
All retained solves reach iteration 300. At the final iteration, the
95th-percentile maximum initial equation residual is $1.27\times10^{-4}$; over
the last 50 iterations, the 95th-percentile spans are 0.0059 for $C_L$ and
$2.65\times10^{-4}$ for $C_D$.

\subsection{Targets and cavitation-risk definition}
The common output vector contains $U_x$, $U_y$, absolute pressure $p$, eddy
viscosity, $k$, $\omega$, pressure coefficient $C_p$, cavitation margin, lift
coefficient $C_L$, and drag coefficient $C_D$. Grid fields use masked
normalized mean-squared error, while the case-level force targets use
normalized mean-squared error. OpenFOAM's kinematic gauge pressure $p_k$ is
converted to absolute pressure as
\begin{equation}
 p=p_\infty+\rho p_k, \qquad C_p=\frac{p-p_\infty}{\tfrac{1}{2}\rho U_\infty^2}.
\end{equation}
For an ambient-pressure value $p_a$, the pressure-based inception margin is
\begin{equation}
 M(x,y;p_a)=p_a+C_p(x,y)\tfrac{1}{2}\rho U_\infty^2-p_v.
\end{equation}
A cell is labeled at risk when $M<0$. We evaluate this label at $p_a\in\{2500,3000,5000,10000,25000,101325\}$~Pa. This sweep creates a useful classification test from the predicted $C_p$ field without representing multiphase physics.

\section{Surrogates and Optimization}
\subsection{Model families}
The \textbf{CNN-U-Net} has three encoder/decoder levels, skip connections,
group normalization, Gaussian error linear unit (GELU) activations, and base
width 16. It maps the complete
input grid to the output grid \cite{ronneberger2015unet}. The \textbf{FNO} has width 32, four operator
blocks, and 12 retained Fourier modes in each direction. One block is
\begin{equation}
h_{\ell+1}=\sigma\!\left(W_\ell h_\ell+
\mathcal{F}^{-1}\!\left(R_\ell\mathcal{F}(h_\ell)\right)\right).
\end{equation}
following the spectral operator construction of \cite{li2021fno}.
Here, $W_\ell$ is a local linear map and $R_\ell$ acts on the retained spectral
modes. The \textbf{DeepONet} uses three-layer, width-64 branch and trunk
networks with 64 basis functions. For output $k$,
\begin{equation}
\widehat{y}_k(\mathbf{x};\boldsymbol{\mu})=
\frac{\mathbf{b}_k(\boldsymbol{\mu})^\top\mathbf{t}_k(\mathbf{x})}{\sqrt{64}}+c_k,
\end{equation}
as in the branch--trunk construction of \cite{lu2021deeponet},
where $\boldsymbol{\mu}$ contains the operating and geometry parameters.

The \textbf{physics-regularized multilayer perceptron (MLP)} has three tanh
layers of width 64. It is
supervised by CFD values at 1,024 sampled points per case. Its extra loss uses
the directional residuals
\begin{equation}
r_c=u_x+v_y,\quad r_x=u u_x+p_x/\rho,\quad r_y=v v_y+p_y/\rho,
\end{equation}
with $\mathcal{L}=\mathcal{L}_{\rm sup}+10^{-4}\mathcal{L}_{\rm phys}$,
following the residual-loss principle in \cite{raissi2019pinn}.
This is a lightweight physics regularizer, not a data-free physics-informed
neural network (PINN) or a complete RANS residual.

Table~\ref{tab:architectures} summarizes the resulting inductive biases. The
two grid models consume the entire Cartesian field at once. DeepONet and the
physics-regularized MLP instead evaluate coordinates, which makes their
representation naturally pointwise, although the present timing benchmark
requests the same complete grid from every model.

\begin{table}[t]
\caption{Surrogate representations and measured inference cost per case.}
\label{tab:architectures}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{llllr}
\toprule
Model & Representation & Principal bias & Params. & ms \\
\midrule
CNN-U-Net & grid & local, multiscale & 485,178 & 10.80 \\
Physics-reg. MLP & points & differential residual & 9,930 & 6.88 \\
FNO & grid & global, spectral & 595,914 & 12.07 \\
DeepONet & branch--trunk & operator basis & 100,746 & 13.52 \\
\bottomrule
\end{tabular}}
\end{table}

All models use AdamW with learning rate $2\times10^{-3}$, weight decay
$10^{-4}$, at most 120 epochs, patience 20, and gradient clipping. We train
each architecture with seeds 7, 17, and 27, always using the same family split.
Normalization is computed from training cases only. The best checkpoint is
selected by validation loss. Model size ranges from 9,930 to 595,914 parameters
(Table~\ref{tab:performance}).

The comparison standardizes data, targets, optimizer, stopping rule, and
evaluation budget, but does not force equal parameter counts. Equalizing width
would not equalize effective capacity across convolutional, spectral,
branch--trunk, and pointwise representations. We therefore report parameter
count, training time, and inference time explicitly and interpret the study as
a comparison of compact canonical configurations, not a capacity-matched
architecture ablation. No hyperparameter is selected on the test families.

Figure~\ref{fig:training} reports the complete optimization histories rather
than only the selected checkpoints. Because the architectures consume the data
in different forms and the objective aggregates heterogeneous normalized
targets, loss magnitude should be interpreted within a model, not as a direct
cross-model accuracy metric. DeepONet reaches its minimum validation objective
at epoch 6, the physics-regularized network at epoch 30, FNO at epoch 54, and
CNN-U-Net at epoch 120. The isolated FNO spikes are retained in the record and
motivate reporting best-checkpoint results alongside the full history.

\begin{figure*}[t]
\centering
\includegraphics[width=0.83\textwidth]{figures/training_loss_curves.png}
\caption{Training and unseen-geometry validation objectives. Curves terminate
after early stopping; objectives combine normalized field and force losses.}
\label{fig:training}
\end{figure*}

Optimization uses dedicated $C_L$ and $C_D$ output heads supervised by
OpenFOAM's total force coefficients. Pressure integration on the coarse common
grid is not used for the objective because it omits viscous traction and
under-resolves the wall.

\subsection{Why physics regularization rather than a full PINN?}
A classical PINN is trained by differentiating its outputs and minimizing the
governing-equation and boundary-condition residuals, sometimes with few or no
solution labels \cite{raissi2019pinn}. Our point network remains primarily
supervised by OpenFOAM values; its low-weight residual contains continuity and
inviscid directional momentum only. It omits viscous stress, Reynolds stress,
the $k$--$\omega$ transport equations, and explicit wall and far-field
residuals. Describing it as a full RANS PINN would therefore overstate the
physics imposed during training.

This lighter comparator was chosen because it applies a recognizable physical
bias while retaining the same labels, split, and modest training budget as the
other surrogates. A full hydrofoil RANS PINN is a different experiment: it
would require coupled turbulence variables and boundary losses, careful
residual balancing near the wall, and substantially more automatic-
differentiation work. The label ``physics-regularized MLP'' keeps that
distinction visible when interpreting its strong pressure results.

\subsection{Common optimization protocol}
At $Re=5\times10^5$, each surrogate drives the same HSO loop. The design
vector $\theta=(m,p,t,\alpha)$ contains maximum camber, camber location,
thickness, and incidence, with bounds $m\in[0,0.09]$, $p\in[0.1,0.9]$,
$t\in[0.06,0.20]$, and $\alpha\in[-4,10]^{\circ}$. We launch Nelder--Mead
\cite{nelder1965simplex} from NACA 0012, 2412, and 4415 and give every run the
same 40-iteration budget. The objective is
\begin{equation}
J(\theta)=\frac{C_L}{|C_D|+10^{-6}}.
\end{equation}
The search calls only the surrogate; OpenFOAM is not used inside the loop.
Cavitation margin is recorded afterward as a secondary diagnostic and does not
alter the objective. We return the best design observed within the
fixed budget, select the strongest of the three starts for each architecture,
and then generate a new mesh and independent OpenFOAM simulation for that
winner.

Although training geometries are discrete NACA families, the model inputs
contain continuous four-digit descriptors $(m,p,t)$, and the optimizer queries
intermediate values within the stated bounds. Some queries extrapolate beyond
the convex coverage of the training shapes, especially near a thickness bound.
We therefore retain every evaluation in the trace and independently remesh and
solve each selected endpoint with CFD. This validates the reported design, not
the full learned objective surface.

For the direct-CFD baseline, the same Nelder--Mead procedure starts from the
same three foils and uses the same bounds, objective, tolerances, and
40-iteration budget, but every evaluation is obtained by remeshing and running
OpenFOAM. This separates endpoint verification, which checks each surrogate's
winner once, from CFD-driven optimization, which supplies a matched reference
search.

\section{Results}
\subsection{Validation accuracy and computational cost}
Table~\ref{tab:performance} reports mean and one sample standard deviation over
three independently trained models on the untouched 64-case NACA 0018/4418
test set. DeepONet gives the lowest mean errors for pressure, $C_p$, $C_L$, and
$C_D$; the physics-regularized network is consistently second and varies less
across seeds. CNN-U-Net is seed-sensitive, especially for the global force
heads, while FNO retains weaker force trends in this low-geometry regime.

\begin{table}[t]
\caption{Final-test RMSE (mean $\pm$ sample standard deviation over three seeds), size, and speedup.}
\label{tab:performance}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lrrrrrr}
\toprule
Model & $p$ (Pa) & $C_p$ & $C_L$ & $C_D$ & Params. & Speedup \\
\midrule
CNN-U-Net & $28.32\pm14.18$ & $0.0657\pm.0373$ & $0.258\pm.200$ & $0.00871\pm.00522$ & 485,178 & $544\pm13$ \\
Physics-reg. MLP & $11.56\pm0.48$ & $0.0445\pm.0012$ & $0.0352\pm.0056$ & $0.00328\pm.00029$ & \textbf{9,930} & \textbf{$867\pm1$} \\
FNO & $27.79\pm1.07$ & $0.0643\pm.0040$ & $0.248\pm.020$ & $0.0103\pm.00054$ & 595,914 & $464\pm6$ \\
DeepONet & \textbf{$10.74\pm1.48$} & \textbf{$0.0411\pm.0045$} & \textbf{$0.0304\pm.0081$} & \textbf{$0.00339\pm.00089$} & 100,746 & $384\pm2$ \\
\bottomrule
\end{tabular}}
\end{table}

Mean training times are 556, 67, 270, and 47~s for CNN-U-Net, the
physics-regularized network, FNO, and DeepONet, respectively. Median OpenFOAM
execution time is 12.4~s per case, while complete-field inference requires
14--32~ms, yielding 384--867$\times$ speedups. The benchmark used an
8-core Apple M2 Mac mini with 16~GB memory; PyTorch 2.13 ran on its central
processing unit (CPU), and single-process OpenFOAM 9 ran in Docker. These
numbers measure repeated local inference and exclude data generation and mesh
construction.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/model_tradeoffs.pdf}
\caption{No architecture dominates every criterion on the 64-case final test set (seed 7 shown for field-level detail).}
\label{fig:tradeoff}
\end{figure}

The ranking also depends strongly on the target field. Figure~\ref{fig:fieldr2}
shows that the pointwise models recover both velocity components and pressure,
whereas CNN-U-Net and FNO are substantially weaker on absolute pressure and
$U_x$. All four models have $R^2\leq0.15$ for eddy viscosity, $k$, and
$\omega$. Thus, good $C_p$ and force predictions do not make these checkpoints
interchangeable with a complete RANS solution; their demonstrated value is in
pressure screening and force-guided design.

\begin{figure*}[t]
\centering
\includegraphics[width=0.78\textwidth]{figures/field_r2_heatmap.pdf}
\caption{Full-field coefficient of determination on the unseen-family
test set (seed 7). The turbulence outputs remain difficult for every model even
when velocity, pressure, or $C_p$ is accurate.}
\label{fig:fieldr2}
\end{figure*}

Figure~\ref{fig:pressure} shows a representative final-test case from the unseen NACA 0018 family. The
pointwise models reproduce the leading-edge pressure structure most closely;
CNN-U-Net smooths the extrema and FNO introduces spatial texture. A common
color scale makes the location and magnitude of those differences directly
comparable across all five panels.

\begin{figure*}[t]
\centering
\includegraphics[width=0.97\textwidth]{figures/heldout_pressure.png}
\caption{Absolute-pressure prediction for a representative NACA 0018
test case (case 179), shown over $x/c\in[-0.25,1.25]$ and
$y/c\in[-0.375,0.375]$. Every panel uses the same color scale.}
\label{fig:pressure}
\end{figure*}

\subsection{Cavitation-inception screening}
The pressure-threshold labels are highly imbalanced, so we report F1 rather
than accuracy. Aggregated over the ambient-pressure sweep, DeepONet obtains
$0.900\pm0.008$, the physics-regularized network $0.867\pm0.008$, CNN-U-Net
$0.758\pm0.198$, and FNO $0.724\pm0.065$. The large CNN-U-Net spread mirrors
its field and force variability. This secondary diagnostic asks whether a
surrogate preserves low-$C_p$ regions; it does not model vapor fraction,
cavity length, or shedding \cite{brennen1995cavitation}.

\begin{table}[t]
\caption{Pressure-threshold diagnostic over the final test set (mean $\pm$
sample standard deviation over three seeds).}
\label{tab:cavitation}
\centering
\begin{tabular}{lrrr}
\toprule
Model & Precision & Recall & F1 \\
\midrule
CNN-U-Net & $0.817\pm.084$ & $0.730\pm.274$ & $0.758\pm.198$ \\
Physics-reg. MLP & $0.959\pm.018$ & $0.791\pm.023$ & $0.867\pm.008$ \\
FNO & $0.615\pm.106$ & $0.892\pm.024$ & $0.724\pm.065$ \\
DeepONet & $0.955\pm.001$ & $0.851\pm.014$ & \textbf{$0.900\pm.008$} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Shape optimization against direct CFD}
Table~\ref{tab:optimization} reports the complete $L/D$ comparison: the
OpenFOAM value of the starting foil, the surrogate estimate at its selected
design, and the result of remeshing that design and solving it once in
OpenFOAM. That one-solve check is endpoint validation. Separately, the direct
baseline places OpenFOAM inside the matched three-start optimizer, providing a
CFD-driven reference under the same local-search budget.

Optimization is also seed-dependent. Across the three selected endpoints per
architecture, medium-grid OpenFOAM gives $L/D=24.44\pm1.35$ for CNN-U-Net,
$37.90\pm1.59$ for the physics-regularized network, $22.10\pm3.46$ for FNO,
and $37.47\pm5.95$ for DeepONet. The matched direct-CFD search reaches 17.80,
27.33, and 29.89 from NACA 0012, 2412, and 4415, respectively. These endpoint
means describe the old medium discretization; the grid study below is needed
before treating their differences as mesh-independent.

\begin{table*}[t]
\caption{Best $L/D$ objective found from each starting foil. Surrogate rows
report learned objective values inside their search; direct CFD reports
OpenFOAM values. Bold entries are the starts selected for endpoint validation.}
\label{tab:allstarts}
\centering
\begin{tabular}{lrrr}
\toprule
Evaluator & NACA 0012 start & NACA 2412 start & NACA 4415 start \\
\midrule
CNN-U-Net & 23.47 & 22.65 & \textbf{25.82} \\
Physics-reg. MLP & 21.77 & \textbf{33.27} & 32.68 \\
FNO & 18.57 & 18.77 & \textbf{20.80} \\
DeepONet & 19.91 & \textbf{60.94} & 59.82 \\
Direct CFD & 17.80 & 27.33 & \textbf{29.89} \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table}[t]
\caption{HSO endpoints over three training seeds. OpenFOAM columns report the
medium-grid mean and sample standard deviation. Grid shift is the seed-7
medium-to-fine $L/D$ change relative to the fine result.}
\label{tab:optimization}
\centering
\resizebox{\columnwidth}{!}{%
\begin{tabular}{lrrrr}
\toprule
Model & Pred. mean & CFD mean & CFD std. & Grid shift \\
\midrule
CNN-U-Net & 21.75 & 24.44 & 1.35 & 18.9\% \\
Physics-reg. MLP & 38.82 & \textbf{37.90} & \textbf{1.59} & 19.6\% \\
FNO & 20.93 & 22.10 & 3.46 & 15.7\% \\
DeepONet & \textbf{51.08} & 37.47 & 5.95 & 19.4\% \\
Direct CFD & -- & 29.89 & -- & -- \\
\bottomrule
\end{tabular}}
\end{table}

\begin{figure*}[t]
\centering
\includegraphics[width=0.94\textwidth]{figures/aso_results.pdf}
\caption{Top: selected shapes against the same dashed NACA 2412 geometric
reference. The common reference supports shape comparison and is not
necessarily the optimizer initializer; actual initializers are listed in
Table~\ref{tab:allstarts}. Bottom: initial-foil OpenFOAM values, surrogate
predictions at the selected designs, fresh OpenFOAM endpoint checks, and the
direct-CFD result.}
\label{fig:optimization}
\end{figure*}

\begin{figure*}[t]
\centering
\includegraphics[width=0.78\textwidth]{figures/optimization_traces.png}
\caption{Best-so-far $L/D$ for all three starts and three training seeds per
surrogate; the direct OpenFOAM panel shows its three starts. Every objective
evaluation is retained in the released CSV traces.}
\label{fig:traces}
\end{figure*}

\section{Discussion}
The repeated runs change the interpretation of the benchmark. DeepONet is the
best final-test predictor, yet its selected $L/D$ varies by 5.95 across seeds;
the physics-regularized network has a slightly lower mean endpoint but only
1.59 standard deviation. CNN-U-Net is unstable in both force prediction and
optimization, while FNO is more repeatable but settles on lower-performing
designs. Predictive accuracy, optimizer ranking, and seed robustness are thus
three separate criteria.

Figure~\ref{fig:traces} shows why all starts matter. The two cambered starts
often enter different basins from NACA 0012, and DeepONet's learned objective
is particularly sharp. The direct run remains a matched local-search baseline,
not a global CFD optimum. Surrogate and direct traces should therefore be read
as budgeted search behavior rather than proof that one evaluator dominates the
continuous design space.

The grid check is the most important qualification. All 12 selected-design
solves converge iteratively by 1,000 iterations, with final residuals below
$10^{-7}$ and negligible late-window force variation. Lift changes by at most
about 4\% from the medium to fine mesh, but drag changes by 16--24\%, producing
16--20\% shifts in $L/D$. Consequently, the endpoint ordering is evidence about
this CFD workflow, not a mesh-independent hydrodynamic optimum. A boundary-layer
mesh and a formal asymptotic grid-convergence study are required before design
deployment.

The field results help explain why parameter count alone is not predictive.
The 9,930-parameter physics-regularized MLP is the smallest model and nearly
matches the best pressure result, while the 595,914-parameter FNO is largest
and ranks third on pressure. In this dataset, an operator model receives only 253
complete training realizations even though each realization contains many grid
cells. The pointwise networks turn those same cases into supervised coordinate
samples and encode the operating parameters directly. This is favorable for a
fixed grid and a low number of geometries, but it does not imply that pointwise
models will dominate as geometric diversity, resolution, or three-dimensional
domain size grows. The comparison instead identifies the inductive bias that
works best at the present data scale.

Runtime should likewise be interpreted at the workflow level. The 384--867
fold online speedups exclude CFD data generation, which is the dominant
up-front expense and must be amortized across operating sweeps or design
studies. They support reuse of a trained surrogate, not replacement of the
simulations needed to construct and verify it.

The water operating conditions also clarify the cavitation result. At
$Re=5\times10^5$ and the stated 1~m chord, $U_\infty=0.5$~m/s and the dynamic
pressure is only about 125~Pa. With nominal ambient pressure near 101~kPa, none
of the optimized designs approaches vapor pressure; cavitation is therefore
reported diagnostically rather than included in the objective. The lower-pressure sweep tests whether the
networks preserve the leading-edge low-pressure pattern under an inception
criterion; it should not be interpreted as a prediction of a developed vapor
cavity. A future cavitation-constrained design study would choose operating
pressure and velocity jointly so that inception is active within the search
space.

Finally, three surrogate winners approach the lower thickness bound. This is a
common place for learned-objective error to matter because the optimizer is
querying continuous shapes that were not simulated during training. The fresh
mesh-and-solve endpoint check answers the immediate question---whether the
selected shape still performs in the reference solver---without claiming that
the learned surface is uniformly accurate around that point. Local CFD samples
around each winner, structural thickness constraints, or a trust-region update
would be natural additions when the objective is a deployable design rather
than the controlled benchmark considered here.

The comparison remains limited to two-dimensional steady RANS and the NACA
four-digit space. Repeated seeds and selected-design grid checks quantify two
important uncertainties here; external experimental force validation and
multiphase cavitation data remain beyond the present benchmark.

\section{Conclusion}
We followed four neural surrogates from RANS-field prediction to a design
decision using a fixed family-level test set and three seeds. DeepONet gives
the lowest mean pressure, $C_p$, lift, and drag errors; the much smaller
physics-regularized network is close and fastest. Complete-field inference is
384--867 times faster than the measured OpenFOAM runs.

The HSO experiment changes the ranking from a simple accuracy table. The
physics-regularized network and DeepONet produce the strongest mean medium-grid
endpoints, but DeepONet varies more across seeds. The matched direct-CFD search
reaches 29.89, showing that a fast surrogate can change not only search
cost but also which basin a budgeted local optimizer reaches. The grid study
also shows that converged iterations do not imply converged drag: medium-to-fine
$L/D$ shifts remain 16--20\%. A scientific surrogate should therefore be judged
on final-test prediction, seed robustness, the consequence of acting on its
output, and the numerical uncertainty of the verifying simulator.

\section*{Artifact Availability}
Code, configurations, split definitions, per-case metrics, optimization traces,
and exact execution commands are public at
\url{https://github.com/aaravvd/hydrofoil-neural-surrogates}. The CFD dataset
and trained checkpoints are archived at
\url{https://doi.org/10.5281/zenodo.21845241}. The repository records the
software environment and checksums needed to connect the source release to the
archived artifacts.

\section*{Acknowledgment and AI-Tool Disclosure}
OpenAI Codex was used throughout manuscript preparation as a general-purpose
writing and code-assistance tool, including language editing, plotting-script
development, and reproducibility checks \cite{openai2026codex}. The author
reviewed the manuscript, reran the reported analyses, and accepts full
responsibility for the scientific claims and submitted text.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
